import Link from "next/link";

export function HomeProductIntro() {
  return (
    <section style={{ padding: "56px 0 34px" }}>
      <div className="container" style={{ display: "grid", gap: 22 }}>
        <div style={{ maxWidth: 900 }}>
          <span className="eyebrow">What is AutoTeams?</span>

          <h1
            style={{
              margin: "12px 0 14px",
              maxWidth: 820,
              fontSize: "clamp(38px, 5vw, 60px)",
              lineHeight: 1.05,
              letterSpacing: "-0.045em",
            }}
          >
            Build better teams with explainable AI.
          </h1>

          <p
            style={{
              margin: 0,
              maxWidth: 860,
              color: "#9aa7ba",
              fontSize: 17,
              lineHeight: 1.7,
            }}
          >
            AutoTeams helps organisations create balanced,
            high-performing teams from the people they already
            have. It combines skills, strengths, team objectives
            and human review to produce explainable recommendations
            rather than black-box decisions.
          </p>
        </div>

        <div style={{ display: "flex", gap: 10, flexWrap: "wrap" }}>
          <Link className="button" href="/get-started">
            Get Started
          </Link>

          <Link className="button secondary" href="/learn">
            Learn More
          </Link>
        </div>

        <div
          style={{
            display: "grid",
            gridTemplateColumns: "repeat(auto-fit,minmax(220px,1fr))",
            gap: 12,
            marginTop: 4,
          }}
        >
          <Feature
            title="Build balanced teams"
            text="Create teams around a real outcome, required strengths and the people available in the workspace."
          />
          <Feature
            title="Explain every recommendation"
            text="Understand confidence, strengths, gaps, risks and why individual people were selected."
          />
          <Feature
            title="Keep the human in control"
            text="Review, compare, approve or reject recommendations with a permanent decision and audit history."
          />
        </div>

        <div
          style={{
            display: "grid",
            gap: 12,
            marginTop: 8,
            padding: 22,
            background: "#171e2d",
            border: "1px solid #2a3448",
            borderRadius: 18,
          }}
        >
          <span className="eyebrow">AutoTeams objectives</span>

          <div
            style={{
              display: "grid",
              gridTemplateColumns: "repeat(auto-fit,minmax(220px,1fr))",
              gap: 10,
            }}
          >
            <Objective number="01" text="Use existing talent more effectively." />
            <Objective number="02" text="Make team selection more consistent and explainable." />
            <Objective number="03" text="Reduce manual effort when forming project and delivery teams." />
            <Objective number="04" text="Support human decisions with evidence, not replace them." />
          </div>
        </div>
      </div>
    </section>
  );
}

function Feature({ title, text }: { title: string; text: string }) {
  return (
    <article
      style={{
        display: "grid",
        gap: 8,
        padding: 18,
        background: "#171e2d",
        border: "1px solid #2a3448",
        borderRadius: 15,
      }}
    >
      <strong style={{ fontSize: 16 }}>{title}</strong>
      <p
        style={{
          margin: 0,
          color: "#8f9bb0",
          fontSize: 13,
          lineHeight: 1.6,
        }}
      >
        {text}
      </p>
    </article>
  );
}

function Objective({ number, text }: { number: string; text: string }) {
  return (
    <div style={{ display: "flex", gap: 10, alignItems: "flex-start" }}>
      <span
        style={{
          display: "grid",
          minWidth: 30,
          height: 30,
          placeItems: "center",
          color: "#cfc9ff",
          background: "rgba(120,104,255,.12)",
          borderRadius: 9,
          fontSize: 9,
          fontWeight: 900,
        }}
      >
        {number}
      </span>

      <span
        style={{
          color: "#c6cedb",
          fontSize: 13,
          lineHeight: 1.55,
        }}
      >
        {text}
      </span>
    </div>
  );
}
